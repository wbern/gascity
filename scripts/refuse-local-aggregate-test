#!/usr/bin/env bash
set -euo pipefail

entrypoint="${1:-aggregate local test suite}"

cat >&2 <<EOF
${entrypoint} is disabled in the wbern fork.

Aggregate local test runs have exhausted this workstation. Use the focused
package-level test guidance in TESTING.md. CI owns broad and sharded coverage.
EOF

exit 2
