{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "gc run cancel --json result",
  "description": "One JSONL record describing the outcome of gc run cancel.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "ok", "command", "run_id", "status", "closed"],
  "properties": {
    "schema_version": {
      "type": "string",
      "description": "Schema version for this command output."
    },
    "ok": {
      "const": true
    },
    "command": {
      "type": "string",
      "description": "The command that produced this result."
    },
    "run_id": {
      "type": "string",
      "description": "The canceled run."
    },
    "status": {
      "type": "string",
      "description": "Run status after the cancel wind-down."
    },
    "closed": {
      "type": "integer",
      "minimum": 0,
      "description": "Count of the run's beads closed by the cancel."
    }
  }
}
