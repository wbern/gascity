#!/usr/bin/env bash
set -euo pipefail

# This fork deliberately disables aggregate local test suites: their fan-out
# can exhaust a developer workstation. Keep the hook non-blocking while CI
# retains broad coverage.
cat >/dev/null
echo "aggregate local test suite disabled in the wbern fork; use focused tests and CI" >&2
